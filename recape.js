var time = '10:05 pm';
var bookPrice = 150;
var iswhiteColor = false;

// Array
var partners = ['sajb', 'mojid', 'nojid', 'akram'];
var elementCount = partners.length;
var nojidIndex = partners.indexOf('nojid');
partners.push('SK SATU');
partners.pop();

// conditionals
if (condition - 1) {
    action - 1;
}
else {
    action - 2;
}

// loop
var i = 0;
while (i <= 17) {
    //    do some work
    i++;
}

for (i = 0; i <= 10; i++) {
    action;
}

// function

function sk(time) {
    if (time >= 19 && time <= 5) {
        return true;
    }
    return true;
}

var A = sk(time);

//use of  "let" and "const"

// value of variable could  
let price = 27;
price = 29;
price = 31;

// variable will not change
const name = 'Sanat kumar';
console.log(name);